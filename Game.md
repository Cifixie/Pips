Levels

Goals (for different levels)

- Collect <n> points within time (ie. 5000 points in 30, 60, 90, 120 seconds)
- Collext <n> pc. of same blocks (ie. 20 purple blocks)
  - Collect <n/x/y> pc. of multiple variances of same block type, (ie. 10 yellow + 15 red blocks)
- Gain <n> -points in total
- Combine <n> -times 5 or 6 blocks by set of moves (ie. )

Survival mode

- Each points equals <n> -amount of time increase on timer
- Each nudge substracts more time from timer
- preselected amount of count-down timer
